spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/person_rest_db?useTimeZone=UTC&serverTimezone=UTC
    username: user
    password: admin123
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
    show-sql: true
    open-in-view: false
logging:
  level:
    #root: WARN
    br.com.viniciussouza: DEBUG
spring-doc:
  paths-to-match: /api/v1/**
  swagger-ui:
    use-root-path: true